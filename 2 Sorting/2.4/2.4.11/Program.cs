namespace _2._4._11
{
    /*
     * 2.4.11
     * 
     * 如果你的应用中有大量插入元素的操作，
     * 但只有若干删除最大元素的操作，
     * 哪种优先队列的实现方法更有效：堆、无序数组、有序数组？
     * 
     */
    class Program
    {
        static void Main(string[] args)
        {
            // 有大量插入操作，选择插入耗时为常数的无序数组实现更好。
        }
    }
}
