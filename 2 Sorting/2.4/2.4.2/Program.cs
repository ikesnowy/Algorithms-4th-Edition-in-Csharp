namespace _2._4._2
{
    /*
     * 2.4.2
     * 
     * 分析以下说法：
     * 要实现在常数时间找到最大元素，
     * 为何不用一个栈或者队列，
     * 然后记录已插入的最大元素并在找出最大元素时返回它的值。
     * 
     */
    class Program
    {
        static void Main(string[] args)
        {
            // 显然这种方法只能取一次最大值。
            // 当需要多次取出最大值时，这个方法就不适用了。
        }
    }
}
