namespace _1._5._10
{

    class Program
    {
        static void Main(string[] args)
        {
            // 答案已经给出，这里简单解释。
            // 输入是 p、q。
            // 现在执行 union，但只是把 p 挂到 q 上而不是挂到 q 的根节点上。
            // 结果显然是整棵树的高度增加了，find 操作的效率会降低。
        }
    }
}
