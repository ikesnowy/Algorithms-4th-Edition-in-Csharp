namespace _3._1._30
{
    /*
     * 3.1.30
     * 
     * 验证。
     * 向 BinarySearchST 加入断言（assert）语句，
     * 在每次插入和删除数据后检查算法的有效性和数据结构的完整性。
     * 例如，对于每个索引必有 i==rank(select(i)) 且数组应该总是有序的。
     * 
     */
    class Program
    {
        static void Main(string[] args)
        {
            // 见 SymbolTable.BinarySearch 的实现
            // 在 Put() 和 Delete() 方法的最后添加 Debug.Assert(Check())
            // 在 Check() 方法中实现题目中的检查要求。
        }
    }
}
